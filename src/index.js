const addNumbers = require('./addNumbers');
const subtractNumbers = require('./subtractNumbers');
const multiplyNumbers = require('./multiplyNumbers');
const divideNumbers = require('./divideNumbers');

console.log(addNumbers(5, 3)); // Outputs: 8
console.log(subtractNumbers(5, 3)); // Outputs: 2
console.log(multiplyNumbers(5, 3)); // Outputs: 15
console.log(divideNumbers(6, 3)); // Outputs: 2